/*
use env::Env;

mod build;
mod ffi_type;
mod functions;
mod lib_;
mod statics;
mod cargo_toml;

pub fn generate(env: &Env) {
    lib_::generate(env);
    build::generate(env);
    cargo_toml::generate(env);
}
*/
