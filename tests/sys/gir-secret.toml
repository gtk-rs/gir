[options]
work_mode = "sys"
girs_dir = "../gir-files"
library = "Secret"
version = "1"
target_path = "./secret-sys"
external_libraries = [
   "Gio",
   "GLib",
   "GObject",
]

ignore = [
   # Appveyor don't have it: added in v0.18.6
   "Secret.SchemaType"
]
